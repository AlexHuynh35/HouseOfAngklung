/*
import { formatDate } from "@/utils/formatDate";
import { Masonry } from "@/components";

type MediaItem = {
  title: string;
  isImage: boolean;
  onYouTube: boolean;
  mediaUrl: string;
};

type GalleryProps = {
  title: string;
  date: string;
  time: string;
  building: string;
  address: string;
  description: string;
  media: MediaItem[];
  hasGallery: boolean;
  path: string;
  photos: number[];
};
*/

const Gallery = (/*{ title, date, time, building, address, description, media, hasGallery, path, photos }: GalleryProps*/) => {
  return (
    <div>
    </div>
  );
};

export default Gallery;